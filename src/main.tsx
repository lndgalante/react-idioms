import React from 'react'
import ReactDOM from 'react-dom/client'

// Import here any idioms for testing
const App = () => null

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
